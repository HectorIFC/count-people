[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --cov=bot --cov=analysis --cov=history --cov=messages --cov-report=term-missing --cov-fail-under=80
markers =
    integration: end-to-end tests with the real models (set RUN_INTEGRATION=1)
